public class e_pt6 {
  public static void printLogicalBST(int current){
    if(current != -1){
      System.out.printf("tree[%d]:data=%d,left=%d,rright=%d\n",currentIdx,tree[currentIdx].data,
      tree[currentIdx].left,tree[currentIdx].right);;
    }
  }
}
